import Navbar from 'react-bootstrap/Navbar';
export default function Header(){
    return(
        <div>
          <Navbar>
            <Navbar.Brand>
              Apartamentos
            </Navbar.Brand>
          </Navbar>
        </div>
      );
}